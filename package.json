{
  "name": "data-overview-screen",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "dev:test": "vite --mode test --host 0.0.0.0",
    "build": "vue-tsc -b && vite build && node scripts/prepare-deploy.mjs test",
    "build:dev": "vue-tsc -b && vite build --mode development && node scripts/prepare-deploy.mjs development",
    "build:test": "vue-tsc -b && vite build --mode test && node scripts/prepare-deploy.mjs test",
    "preview": "vite preview --host 0.0.0.0",
    "type-check": "vue-tsc -b --force"
  },
  "dependencies": {
    "axios": "^1.20.0",
    "echarts": "^5.6.0",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.7.2",
    "vite": "^6.0.5",
    "vue-tsc": "^2.2.0"
  }
}
